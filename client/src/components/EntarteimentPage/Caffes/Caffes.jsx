import React from 'react';
import PropTypes from 'prop-types';
import styles from './Caffes.module.scss';

const Caffes = () => (
  <div className={styles.Caffes} data-testid="Caffes">
    Caffes Component
  </div>
);

Caffes.propTypes = {};

Caffes.defaultProps = {};

export default Caffes;
